---
일자: 2026-05-15
종류: 운영 규칙
주제: 입수 패키지 작성 규칙 — 모바일 디스패치 + Claude Code 작업 흐름 반영
태그: [운영규칙, 입수규칙, ClaudeCode, 옵시디언, 모바일]
---

# 입수 패키지 작성 규칙 — 모바일 디스패치 + Claude Code 작업 흐름

## 1. 박준호의 실제 작업 흐름 (정확한 그림)

1. 박준호가 핸드폰에서 클로드 챗 사용
2. 입수 패키지 받음
3. 핸드폰 디스패치로 PC 원격 제어
4. PC 옵시디언 vault 폴더의 터미널에서 Claude Code 실행 중
5. 클로드 챗 입수 패키지를 Claude Code에 통째로 붙여넣기
6. Claude Code가 박준호 vault 보고 자동 분배·파일 생성·갱신·git push

핵심 제약:
- 박준호가 PC 앞에 없음 (모바일 디스패치 사용)
- 모바일 환경에서 박준호 추가 입력·왔다갔다 작업 부담 큼
- 한 번의 붙여넣기 + 짧은 명령으로 모든 작업이 끝나야 함

박준호 자기 진술:

> "최대한 작업방식이 간단명료하고 시간을 뺏지 않았으면 좋겠어 여러번 일 안하고 한번의 명령으로 처리될수있게 말이야"

## 2. 입수 패키지 작성 절대 원칙

### 원칙 1: 한 덩어리 마크다운 (절대)

- 블록 여러 개로 나누지 않음
- 코드블록 중첩으로 박준호 혼란 주지 않음
- 박준호 복사 1회 = 모든 내용 포함

### 원칙 2: 자기완결적 (Self-contained)

- 입수 패키지 안에 Claude Code가 작업할 모든 정보 포함
- 박준호 추가 설명·추가 명령 없이도 Claude Code가 처리 가능
- 박준호 후속 명령은 "위키에 반영해줘" 한 줄이면 끝

### 원칙 3: Claude Code가 판단 가능한 구조

- frontmatter로 메타데이터 명시
- 영향받을 페이지 명시
- 색인·로그 갱신안 한 줄씩 포함
- Claude Code가 박준호 vault 구조 + 입수 패키지 정보로 분배 판단 가능

### 원칙 4: 박준호 작업 부담 0

- 박준호가 폴더 만들기·파일명 결정·위치 판단 X
- 박준호는 복사 → 붙여넣기 → 짧은 명령 한 번

## 3. 모바일 디스패치 환경 추가 고려사항

- 박준호가 디스패치 통해 PC 화면 보면서 작업하지만, 모바일에서 텍스트 입력은 느림
- 따라서 박준호가 Claude Code에 추가로 타이핑할 내용은 최소화
- 입수 패키지 자체에 "이걸 위키에 반영해줘"라는 의도가 frontmatter나 구조로 드러나야 함
- 박준호가 "위키에 반영해줘" 정도만 타이핑하면 끝나는 구조

## 4. 표준 입수 패키지 구조

다음 섹션들을 한 마크다운 안에 순서대로 포함:

1. frontmatter (일자, 종류, 주제, 태그)
2. 제목 (대화 핵심 한 줄)
3. 박준호 자기 진술 원문 (다듬지 않고 그대로)
4. 대화 흐름 요약 (시간순 또는 논리순 짧게)
5. 핵심 발견·결정 (이번 대화의 결정적 내용)
6. 박준호 자기 시스템 작동 사례 (해당 시)
7. 영향받을 위키 페이지 (갱신 후보 + 신규 후보)
8. 색인 갱신안 (한 줄)
9. 로그 갱신안 (한 줄)
10. 다음 대화 보강 권장 (선택)

## 5. 박준호의 작업 흐름 (단순화된 최종 형태)

박준호의 입수 작업은 다음 4단계로 끝:

1. 클로드 챗에서 입수 패키지 받음
2. 마크다운 전체 복사 (한 번)
3. 디스패치로 PC Claude Code 창에 붙여넣기
4. "위키에 반영해줘" 짧은 명령 입력

이후 Claude Code가 자동 처리:
- 새 파일 생성
- 기존 파일 갱신
- 색인·로그 업데이트
- git add + commit + push

박준호는 4단계 이후 작업 없음.

## 6. 향후 클로드 챗 세션이 지킬 절대 규칙

박준호가 "입수해줘" / "위키에 저장" / "이거 위키에 넣어" 트리거 사용 시:

반드시 해야 할 것:
- 한 덩어리 마크다운으로 작성 (단일 코드블록)
- frontmatter 포함
- 박준호 자기 진술 원문 보존
- 영향받을 페이지 + 색인·로그 갱신안 명시
- 코드블록 안에 또 다른 코드블록 중첩하지 않음 (박준호 복사 혼란 방지)

절대 하지 말 것:
- 여러 블록으로 나누기
- 박준호에게 폴더·파일명 결정 요구
- 박준호에게 "어디에 붙여넣으세요" 안내
- 박준호가 손으로 분배해야 하는 형태로 제공
- 박준호 추가 입력·확인·왔다갔다 요구

## 7. 예외 상황

박준호가 명시적으로 다른 방식을 요청하는 경우에만 예외:
- "손으로 정리할 거니까 블록 나눠줘"
- "PC에서 직접 작업할 거야"
- 기타 박준호 명시 요청

기본은 항상 한 덩어리 마크다운.

## 8. 영향받을 위키 페이지

### 신규 페이지 후보

- 운영규칙/입수패키지_작성규칙 — 모바일 디스패치 + Claude Code 흐름 반영 입수 규칙

### 갱신 후보 (있을 경우)

- AI/Claude_응답_규칙_사실기반 — 입수 형식 항목 추가
- 운영규칙 폴더 내 기존 클로드 운영 페이지

기존 페이지 발견 시 이 규칙으로 통합·갱신.

## 9. 색인 갱신안

색인 추가 한 줄:
- 입수 패키지 작성 규칙 — 모바일 디스패치 + Claude Code 흐름 / 한 덩어리 마크다운 / 한 번의 명령

## 10. 로그 갱신안

로그 추가:

## [2026-05-15] 입수 | 운영 규칙: 입수 패키지 작성 규칙 정립 (모바일 디스패치 + Claude Code 흐름)

## 11. 다음 대화 보강 권장

- 박준호의 디스패치 환경 구체 정보 (어떤 디스패치 도구, PC OS 등) 정리
- Claude Code 자주 쓰는 명령 패턴 정리
- 박준호 vault 실제 폴더 구조 위키에 기록 (향후 클로드 세션이 참고 가능)
